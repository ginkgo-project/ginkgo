{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "spmv-distributed.reuse.json",
  "title": "SpMV",
  "desciption": "something",
  "type": "object",
  "properties": {
    "format": {
      "type": "string",
      "pattern": "^[a-z]+-[a-z]+"
    },
    "reorder": {
      "enum": [
        "natural"
      ]
    }
  },
  "required": [
    "format"
  ]
}
