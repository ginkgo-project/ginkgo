check_stage:
  stage: check


build_stage:
  stage: build
  extends:
    - .is_interruptible
  script:
    - echo "something"
    - echo "${SET_INTERRUPTIBLE}"
    - sleep 10s
    - echo "woke up"
  tags:
    - private_ci
    - status-jobs
